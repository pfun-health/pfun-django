# pfun-django
django management layer for pfun digital health

## Usage

```bash

# change to app directory
$ cd ./app

# Install, setup venv with uv:
$ uv sync && uv pip install -e .

# Run the development server
$ python manage.py runserver

```
